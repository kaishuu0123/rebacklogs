# 2026-03-16 作業ログ

## 完了タスク

### ② vite_rails + turbo-mount 導入

**方針**: Webpacker は残したまま Vite を並走させる段階移行。

#### 追加した gem
- `vite_rails ~> 3.0` — Rails × Vite 統合
- `turbo-mount 0.4.4` — React コンポーネントを Turbo で mount するブリッジ

#### 追加した npm パッケージ
| パッケージ | バージョン | 用途 |
|---|---|---|
| vite | 8.0.0 | ビルドツール |
| vite-plugin-ruby | 5.1.3 | Rails との統合 |
| @vitejs/plugin-react | 6.0.1 | React/JSX サポート |
| @hotwired/turbo | 8.0.23 | Turbolinks の後継（turbo-mount 必須） |
| turbo-mount | 0.4.4 | npm 側（React コンポーネント登録） |
| react / react-dom | 19.2.4 | React 本体 |
| typescript | 5.9.3 | 型チェック |
| @types/react / @types/react-dom | 19.x | 型定義 |

#### 生成・作成ファイル
- `vite.config.mts` — RubyPlugin + React プラグイン設定
- `config/vite.json` — vite_rails 設定（port: 3036、entrypoints: app/javascript）
- `tsconfig.json` — TypeScript 設定（`~/` エイリアス対応）
- `app/javascript/entrypoints/application.ts` — Turbo 起動 + turbo-mount 初期化
- `app/javascript/components/react/HelloWorld.tsx` — 動作確認用サンプル

#### レイアウト変更
`app/views/layouts/application.html.erb` に追加:
```erb
<%= vite_client_tag %>
<%= vite_typescript_tag 'application' %>
```
Webpacker の `javascript_packs_with_chunks_tag` と**共存**。

#### 検証
```
bundle exec vite build  →  ✓ built in 440ms（エラーなし）
```

---

### ③ Biome セットアップ（linter + formatter）

**採用理由**: VitePlus（https://viteplus.dev/）と比較検討した結果、
VitePlus は `vite_rails` gem との統合実績が不明なため Biome を採用。

#### 追加
- `@biomejs/biome 2.4.7` (devDependencies)

#### 設定ファイル
`biome.json`:
- 対象: `app/javascript/entrypoints/**` と `app/javascript/components/react/**`
- インデント: スペース 2
- クォート: シングル
- trailing commas: on
- import 自動ソート: on

#### npm scripts
```json
"lint":     "biome check ...",
"lint:fix": "biome check --write ...",
"format":   "biome format --write ..."
```

#### 検証
```
yarn lint     →  Checked 3 files. No fixes applied.（エラーなし）
yarn lint:fix →  Fixed 3 files.（セミコロン・import 順など自動修正）
```

---

### ③（続き）Tailwind CSS v4 + shadcn/ui セットアップ

#### Tailwind CSS v4
- `tailwindcss` + `@tailwindcss/vite` インストール
- `vite.config.mts` に `tailwindcss()` プラグイン追加
- `application.css` に `@import "tailwindcss"` 追加

#### shadcn/ui
- `npx shadcn@latest init --defaults` で自動初期化（Vite + Tailwind v4 を自動検出）
- `components.json` 生成（`~/` エイリアスで `app/javascript/` を指す）
- `app/javascript/components/ui/button.tsx` — 初期コンポーネント
- `app/javascript/lib/utils.ts` — `cn()` ユーティリティ
- `application.css` に CSS Custom Properties（`--primary` 等）が追記済み
- Geist フォント（`@fontsource-variable/geist`）が自動導入

#### Biome に CSS サポート追加
- `biome.json` に `css.parser.tailwindDirectives: true` を設定
  （`@custom-variant`, `@theme inline`, `@apply` 等の Tailwind v4 構文を許可）

#### 検証
```
bundle exec vite build  →  ✓ CSS 29KB（Tailwind + CSS Custom Properties + Geist フォント）
yarn lint               →  Checked 6 files. No fixes applied.
```

---

### 共通パーツ Bootstrap → Tailwind/shadcn 移行（計画 C）

CLAUDE.md の計画 ④ の前提として、共通パーツを先に移行する方針を決定。

#### CSS 干渉対策
- Tailwind の `@layer base` を `.react-root` スコープに限定
- Bootstrap コンポーネントへの干渉を防ぐ（移行完了後に `body` スコープに戻す）

#### shadcn コンポーネント追加
- `dropdown-menu`, `collapsible`, `separator`
- `lucide-react` アイコンライブラリ追加

#### 作成した React コンポーネント
| コンポーネント | ファイル | 置き換え対象 |
|---|---|---|
| `Header` | `components/react/Header.tsx` | `layouts/_header.html.erb` + `_user_menu.html.erb` |
| `ProjectSidebar` | `components/react/ProjectSidebar.tsx` | `commons/_sidebar.html.erb` |

#### ERB の変更
- `layouts/_header.html.erb` → `turbo_mount "Header"` で React コンポーネントをマウント
- `commons/_sidebar.html.erb` → `turbo_mount "ProjectSidebar"` で React コンポーネントをマウント
- `entrypoints/application.ts` に両コンポーネントを登録

#### 備考
- sign out リンクは `data-turbo-method="delete"` に移行（Rails UJS → Turbo）
- `@types/hotwired__turbo` 追加（TypeScript 型エラー解消）

---

## 現在の状態

| # | タスク | 状態 |
|---|---|---|
| ① | SQLite → PostgreSQL 統一 | ✅ 完了 |
| ② | vite_rails + turbo-mount 導入 | ✅ 完了 |
| ③ | React + TypeScript + Tailwind + shadcn + Biome | ✅ 完了 |
| C | 共通パーツ（Header / ProjectSidebar）Bootstrap → React/Tailwind | ✅ 完了 |
| ④ | ページ単位で Vue → React 置き換え | 未着手 |
| ⑤ | Webpacker / Bootstrap / Vue 削除 | 未着手（④完了後） |
| ⑥ | ブランドカラー設定機能の実装 | 未着手 |
| ⑦ | テスト整備 | 未着手 |

## 次回の作業

共通パーツの残り:
- `application_settings/common/_sidebar.html.erb` — 管理画面サイドバー（Tailwind 直書き）
- Kaminari ページネーション — Tailwind クラスに置き換え

その後 ④ ページ単位の Vue → React 置き換えへ。
最初のページ候補（シンプルなものから）:
- 設定画面 (`/projects/:id/settings`) — Vue コンポーネントが比較的小さい

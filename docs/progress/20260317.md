# 2026-03-17 作業ログ

## 完了タスク

### ④ ページ単位で Vue → React 置き換え（全ページ完了）

前回（2026-03-16）に引き続き、残り全ページを一括移行。

#### 移行したページ

| ページ | Vue エントリー | React コンポーネント |
|---|---|---|
| プロジェクト設定 | `packs/project_settings.js` | `react/ProjectSettings/index.tsx` |
| バックログ | `packs/backlogs.js` | `react/Backlogs/index.tsx` |
| クローズドスプリント | `packs/closed_sprints.js` | `react/ClosedSprints/index.tsx` |
| カンバン | `packs/kanban.js` | `react/Kanban/index.tsx` |

#### 新規作成した共有コンポーネント

- `react/shared/types.ts` — TypeScript 型定義（Story, Sprint, Task, Comment, History 等）
- `react/shared/colorUtils.ts` — `idealTextColor()` / `categoryBadgeStyle()` の React 版
- `react/shared/MarkdownContent.tsx` — `react-markdown` ラッパー
- `react/shared/TicketModal.tsx` — Story / Task 共通モーダル（~820行）

#### 使用ライブラリ

- `@dnd-kit/core` + `@dnd-kit/sortable` — ドラッグ&ドロップ
- `@tanstack/react-query` v5 — サーバー状態管理
- `sonner` — トースト通知
- `react-markdown` — Markdown レンダリング
- `react-colorful` — カラーピッカー（ProjectSettings）

---

### ⑤ Webpacker / Bootstrap / Vue 削除

全ページ移行完了を受けて実施。

#### 削除した JS ファイル・ディレクトリ

| 削除対象 | 理由 |
|---|---|
| `app/javascript/packs/` ディレクトリ（全削除） | Webpacker 不要 |
| `app/javascript/pages/` | Vue ページコンポーネント |
| `app/javascript/stores/` | Vuex ストア |
| `app/javascript/components/backlogs/` | Vue バックログコンポーネント |
| `app/javascript/components/kanban/` | Vue カンバンコンポーネント |
| `app/javascript/components/commons/` | Vue 共通コンポーネント |
| `app/javascript/mixins/` | Vue mixin（colorUtils.js 等） |
| `app/javascript/i18n/` | Vue 用 i18n |
| `app/javascript/commons/` | Vue 共通 JS |
| `app/javascript/stylesheets/` | Bootstrap SCSS（application.scss） |
| `app/javascript/src/` | sb-admin-2.js 等 |
| `app/javascript/channels/` | Action Cable（未使用） |
| `app/javascript/app.vue` | Vue ルートコンポーネント |
| `app/javascript/colorUtils.js` | Vue mixin のルートコピー |
| `app/views/layouts/_user_menu.html.erb` | React Header に統合済み |

#### 削除した npm パッケージ

Bootstrap 系: `bootstrap`, `bootstrap-select`, `bootstrap-vue`, `bs-custom-file-input`, `popper.js`
Vue 系: `vue`, `vue-axios`, `vue-clipboard2`, `vue-color`, `vue-i18n`, `vue-loader`, `vue-loading-overlay`, `vue-numeral-filter`, `vue-observe-visibility`, `vue-router`, `vue-select`, `vue-template-compiler`, `vue2-daterange-picker`, `vuedraggable`, `vuex`
その他: `jquery`, `toastr`, `turbolinks`, `@rails/webpacker`, `markdown-it`, `markdown-it-ins`, `markdown-it-sanitizer`, `highlight.js`, `url-parse`, `install`, `@fortawesome/fontawesome-free`（webfont版 → SVG版を継続使用）
devDependencies: `webpack-bundle-analyzer`, `webpack-dev-server`

#### ERB レイアウト変更

**`layouts/application.html.erb`**:
- `stylesheet_pack_tag` / `javascript_packs_with_chunks_tag` を削除
- `FlashToaster` を `turbo_mount` でマウント（toastr 置き換え）
- `.flash` hidden div 方式を廃止

**`layouts/installer.html.erb`**:
- Webpacker タグ削除 → Vite タグ追加

#### プロジェクトビューの Bootstrap レイアウト除去

`show.html.erb`, `closed_sprints.html.erb`, `kanban.html.erb` の `#wrapper` / `#content-wrapper` (Bootstrap/sb-admin-2) を Tailwind `flex` レイアウトに変換:

```erb
<div class="flex min-h-[calc(100vh-3.5rem)]">
  <%= render 'commons/sidebar', { project: @project } %>
  <div class="flex-1 min-w-0">
    <%= turbo_mount "Backlogs", props: { ... } %>
  </div>
</div>
```

#### その他の変更

- `application.css` の Bootstrap 共存ハック（`.react-root *` スコープ制限、`!important` オーバーライド群）をすべて削除
- `@layer base` を `body` スコープに変更（全体適用）
- Font Awesome を Webpacker SCSS から Vite CSS に移行（`@import "@fortawesome/fontawesome-free/css/all.min.css"`）
- `postcss.config.js` を空化（Webpacker 用プラグインを削除）
- `link_to method: :delete` → `data: { turbo_method: :delete }` 変換（6ファイル）
- `data: { confirm: }` → `data: { turbo_confirm: }` 変換（2ファイル）

#### 新規作成

- `app/javascript/components/react/FlashToaster.tsx` — Sonner ベースのフラッシュメッセージ

#### 検証結果

```
npx vite build → ✓ built in 677ms（エラーなし）
yarn lint      → Checked 31 files. Found 0 errors. Found 7 warnings.
```

---

## 現在の状態

| # | タスク | 状態 |
|---|---|---|
| ① | SQLite → PostgreSQL 統一 | ✅ 完了 |
| ② | vite_rails + turbo-mount 導入 | ✅ 完了 |
| ③ | React + TypeScript + Tailwind + shadcn + Biome | ✅ 完了 |
| ④ | ページ単位で Vue → React 置き換え | ✅ 完了 |
| ⑤ | Webpacker / Bootstrap / Vue 削除 | ✅ 完了 |
| ⑥ | ブランドカラー設定機能の実装 | 未着手 |
| ⑦ | テスト整備 | 未着手 |

## 次回の作業

⑥ ブランドカラー設定機能の実装:
- CSS Custom Properties (`--primary` 等) を DB 設定で上書きする方式
- ブランドカラー1色から `idealTextColor()` 相当でテキスト色を自動計算
- ApplicationSettings ページに設定 UI を追加

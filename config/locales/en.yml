# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   'true': 'foo'
#
# To learn more, please read the Rails Internationalization guide
# available at https://guides.rubyonrails.org/i18n.html.

en:
  hello: "Hello world"
  actions:
    sign_in: Sign in
    sign_out: Sign out
    create_project: Create project
    edit_project: Edit project
    back: Back
    upload: Upload
    user_settings: User settings
    remove: Remove
    settings: Settings
  application_settings:
    common:
      sidebar:
        general_settings: General Settings
        group_managements: Group Managements
        user_managements: User Managements
    dashboard:
      index:
        title: General Settings
        site_settings: Site Settings
        theme_settings: Theme
        theme_description: Choose a color theme for the entire application.
    group_managements:
      index:
        title: Group Managements
        create_group: Create Group
      new:
        create: Create Group
      show:
        title: Group Managements
        add_users_to_group: Add user(s) to the group
        select_user: Select User...
        add_user: Add
        users: Users
    user_managements:
      index:
        title: User Managements
      edit:
        title: User Managements
        current_image: Current Image
        role: Role
  role:
    description:
      admin: Authority that allows all operations
      developer: Permission to create projects and operate only the projects to which they belong
  activerecord:
    attributes:
      project:
        is_public: Public Project?
      group:
        name: Group name